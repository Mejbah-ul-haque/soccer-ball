import React from 'react';
import "./NotFound.css";

const NotFound = () => {
    return (
        <div>
            <img src={require('../Images/notFound.png')} />
        </div>
    );
};

export default NotFound;